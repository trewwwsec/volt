# Pilot Metrics Log - 2026-03-22

Reference runbook: [docs/PILOT_RUNBOOK.md](PILOT_RUNBOOK.md)
Reference bundle: `/tmp/subrecon-pilot-20260322-231020`

## Session Metadata

- Date: 2026-03-22
- Operator: nate
- Subrecon version: `subrecon 0.1.0`
- Git commit: `5a12af5fff1841430b79c500aec9bacb3bb697cc`

## Target Run Records

| Target | Run Type | Command Ref | Duration (s) | Total Findings | Degraded Sources (`partial/error`) | Notes |
| --- | --- | --- | ---: | ---: | --- | --- |
| iana.org | core | `core_e2e_iana` | n/a | 37 | `search=error` | Common Crawl index endpoint resolution failed in this run; CT + GCS + takeover paths still produced expected output. |
| toolbox2.test | s3-only | `s3_only_canary` | n/a | 1 | none | Passive S3 canary target auto-selected and detected as expected. |
| gcp-public-data.test | gcs-only | `gcs_only_landsat` | n/a | 2 | none | GCS signal target detected as expected. |
| iana.org | ct-only | `ct_only_iana` | n/a | 21 | none | CT path stable in this run. |
| iana.org | search-only | `search_only_iana` | n/a | 0 | `search=error` | Search provider path degraded (`bing_challenge_page` + `commoncrawl_index_unavailable`) in this run. |
| iana.org | takeover+ct | `takeover_ct_iana` | n/a | 21 | none | Takeover path `ok_no_results`, CT inventory present. |
| iana.org | subfinder-only | `subfinder_only_iana` | n/a | 65 | none | Subfinder path stable in this run. |
| iana.org | amass-only | `amass_only_iana` | n/a | 0 | `amass=partial` | Local amass lacks `-src`/`-json`; compatibility mode ran with plain output and returned no hosts. |

## Launch Gate Checklist

| Gate | Pass/Fail | Evidence |
| --- | --- | --- |
| Installability (`uv tool` + `pipx`) | Pass | Validated in clean temp environments during Phase 5 work; `subrecon --version` and `--help` functional. |
| Deterministic quality gates | Pass | `ruff check`, `ruff format --check`, compile, and `115` unit tests all passed in this run. |
| Reliability guidance behavior | Pass | Degraded sources emitted deterministic warning block and `operator_action` notes. |
| Onboarding docs clarity | Pass | Quickstart + triage + support + contributing + pilot runbook available and linked in README. |
| Pilot operator self-sufficiency | Pass (with caveats) | Full matrix executed via `scripts/run_pilot_test.sh` without manual code changes; caveats limited to upstream search/amass variability. |

## Decision Record

- Decision: `GO` (release-candidate readiness) with caveats.
- Rationale: Core product paths validated end-to-end; deterministic gates clean; passive S3/GCS canary checks stable.
- Caveats:
  - `search` can degrade to `error` when Common Crawl index endpoint resolution fails and Bing returns challenge pages.
  - `amass` remains environment/version-sensitive; compatibility mode may still yield zero hosts.
- Follow-up Actions:
  - Keep amass as optional enrichment and treat amass-only negatives as inconclusive when degraded.
  - Continue monitoring search-provider drift via existing non-blocking live-smoke workflow.
